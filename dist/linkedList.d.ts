import { LinkedListResult, TreeNode } from './types';
export declare const addNode: (treeNode: TreeNode, high: number, id: string) => boolean;
export declare const removeNode: (treeNode: TreeNode, high: number, id: string) => LinkedListResult;
